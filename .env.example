# Set ONE key to select the provider.
# If both are set, the agent will ask which to use at startup.

OPENAI_API_KEY=       # → OpenAI agents SDK   (default model: gpt-4.5)
ANTHROPIC_API_KEY=    # → Anthropic Claude SDK (default model: claude-opus-4-7)
